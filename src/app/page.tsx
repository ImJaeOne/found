import { Button } from '@/ui/shadcn/button';

const Home = async () => {
  return (
    <>
      <Button variant="button" size="button">
        aaa
      </Button>
      <Button variant="label" size="label">
        bbb
      </Button>
      <Button>ccc</Button>
    </>
  );
};

export default Home;
